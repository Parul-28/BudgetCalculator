<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.parul.budgetcalculator">

    <application
        android:allowBackup="true"
        android:icon="@drawable/vec"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".signup">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login" />
        <activity android:name=".database" />
        <activity android:name=".BudgetDialog" />
        <activity android:name=".add_database" />
        <activity android:name=".money_used" />
        <activity android:name=".add_money" />
        <activity android:name=".listview" />
        <activity android:name=".CustomeAdapter" />
        <activity android:name=".edit_fields" />
        <activity android:name=".price_database"></activity>
    </application>

</manifest>